{
  "name": "devops-console",
  "version": "1.0.0",
  "description": "Internal Developer Portal — DevOps & MLOps Console",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend run dev & npm --prefix frontend start",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}
